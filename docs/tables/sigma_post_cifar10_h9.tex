% STATUS: CIFAR-10 holdout 9, distance pool, 95th-pct.\ cut; 4 arms; each fraction scored in its own fine-tuned space (2 rounds); 200 null / 50 signal toys, $N_D{=}5000$; purity = round-1 pool purity at $f{=}0.03$; $\dagger$ = the label-free cut declined at some fraction below the crossing (the space is then the frozen one at that fraction); purity `--' = declined at $f{=}0.03$; an `($n$/$m$)' row is an arm still running.
\begin{table}[t]
\centering
\footnotesize
\begin{tabular}{lcccccccc}
\toprule
arm & frozen SparKer & purity & Eucl. & Eucl.\,(anch.) & Maha. & MMD & SparKer & SparKer\,(anch.) \\
\midrule
SupCon & 0.018 & 0.02 & \textbf{0.008} & 0.010 & 0.017 & 0.018 & 0.010 & 0.009 \\
\addlinespace
SupCon+SIGReg ($\lambda{=}5$) & 0.032 & 0.01 & 0.025 & \textbf{0.016} & 0.040 & 0.040 & 0.025 & 0.025 \\
\addlinespace
NPLM-dist.\ (sup.) & 0.024 & 0.14 & 0.017 & 0.014 & 0.015 & 0.018 & 0.014 & \textbf{0.009} \\
\addlinespace
NPLM-dist.+classwise SIGReg & 0.028 & 0.17 & 0.016 & 0.022 & \textbf{0.016} & 0.029 & 0.017 & 0.017 \\
\bottomrule
\end{tabular}
\caption{\textbf{Post-discovery $f^\star(2\sigma)$ on the distance pool, 95th-pct.\ cut.} The frozen-space SparKer $f^\star$ is repeated for reference; bold marks the best post-discovery test per arm. \emph{Eucl.\,(anch.)} is the mean of $d_{\rm seen}-d_{\rm disc}$ and \emph{SparKer\,(anch.)} seeds the kernels at the discovered anchors; both consume the anchors discovery planted, the other four do not. \emph{Coverage:} CIFAR-10 holdout 9, distance pool, 95th-pct.\ cut; 4 arms; each fraction scored in its own fine-tuned space (2 rounds); 200 null / 50 signal toys, $N_D{=}5000$; purity = round-1 pool purity at $f{=}0.03$; $\dagger$ = the label-free cut declined at some fraction below the crossing (the space is then the frozen one at that fraction); purity `--' = declined at $f{=}0.03$; an `($n$/$m$)' row is an arm still running.}
\label{tab:sigma_post_cifar10_h9_quantile}
\end{table}
